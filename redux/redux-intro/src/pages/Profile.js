import React from 'react';

export default function Profile () {
    return (
        <div style={{ backgroundColor: 'red', color: 'white', maxWidth: '4em' }}>
            Profile
        </div>

    );
}
